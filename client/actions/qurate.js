import { createAction } from 'redux-act'

export const stepForward = createAction('Step forward')
export const restartQuiz = createAction('Restart quiz')
export const resetQurate = createAction('Reset qurate')
